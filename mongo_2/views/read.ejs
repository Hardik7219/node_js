<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="/mongo_2/public/stylesheets/style.css">
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
</head>
<body>
    <div class=" min-h-screen w-full bg-black p-10">
        <a href="/" class="text-blue-300 mb-10 block">home</a>
        <h1 class="text-white m-10">users</h1>
        <div class="flex gap-3 flex-wrap">
                <% if(users.length>0) { %>
                    <% users.forEach(function(user){ %>
                        <div class=" read text-white bg-blue-500 p-2 w-70 ">
                            <div class="img w-66 h-50 bg-white">
                                <img class="w-full h-full object-cover" src="<%= user.image %>" alt="user image"/>
                            </div>
                            <div class="info" >
                                <h1>USERNAME : <%= user.userName %> </h1>
                                <h1>EMAIL: <%= user.email %></h1>
                            </div>
                            <div class="m-1">
                                <a href="/delete/<%= user._id %> " class="bg-red-500  rounded-md text-white">delete user</a>
                                <a href="/edit/<%= user._id %>" class="bg-green-500  rounded-md text-white">edit user</a>
                            </div> 
                        </div>
                    <% })} else { %>
                        <h1 class="text-white">no users</h1>
                <% }; %>
            
            </div>
            
        </div>
    </div>
</body>
</html>